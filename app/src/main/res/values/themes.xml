<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base app theme. Compose drives all real UI; this just sets window chrome
         so the black cosmic background shows immediately (no white flash on cold start). -->
    <style name="Theme.FrostByte" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowLightStatusBar" tools:targetApi="23">false</item>
        <item name="android:forceDarkAllowed" tools:targetApi="29">false</item>
    </style>
</resources>
